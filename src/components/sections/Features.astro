---
import BtnLink from "../shared/BtnLink.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import Title from "../shared/Title.astro";
import InstagramEmbed from "../elements/InstagramEmbed.astro";
---

<section id="features" class="relative py-20">
  <Container className="flex flex-col midmd:flex-row gap-10 lg:gap-12">
    <!-- Left: text, description, features -->
    <div class="flex-1 flex midmd:w-7/12 lg:w-1/2 flex-col">
      <Title> What makes Tzur Combat Arts different? </Title>

      <Paragraph className="mt-8">
        TCA Academy in Colvale, Goa combines traditional martial
        arts with modern fitness and performance coaching. Our programs are
        designed to deliver measurable results — improved fitness, practical
        self-defense, and stronger confidence for kids, adults, and
        professionals.
      </Paragraph>

      <Paragraph className="mt-4">
        We focus on safe progress, structured curriculum, and a supportive
        community so students keep improving and stay motivated for the long
        run.
      </Paragraph>

      <!-- Feature list (responsive grid) -->
      <ul
        class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-4 text-heading-3 font-medium"
      >
        <li class="flex items-start gap-3">
          <span
            class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary"
          >
            <!-- check SVG -->
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span>Certified instructor (YKKF & competitive background)</span>
        </li>

        <li class="flex items-start gap-3">
          <span
            class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary"
          >
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span>Structured curriculum & belt progression (kids & adults)</span>
        </li>

        <li class="flex items-start gap-3">
          <span
            class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary"
          >
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span>Flexible class times — 6 AM to 8 PM</span>
        </li>

        <li class="flex items-start gap-3">
          <span
            class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary"
          >
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span
            >Small class sizes & personal coaching options for faster progress</span
          >
        </li>

        <li class="flex items-start gap-3">
          <span
            class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary"
          >
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span
            >Safety-first training: warmups, progressions, and injury-aware
            coaching</span
          >
        </li>

        <li class="flex items-start gap-3">
          <span class="flex-none bg-box-bg rounded-full w-8 h-8 mr-2 inline-flex justify-center items-center text-primary">
            <svg
              xmlns="https://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5 13l4 4L19 7"></path>
            </svg>
          </span>
          <span>Free trial class & easy enrollment — book via WhatsApp or visit us</span>
        </li>
      </ul>

      <!-- Social Proof / Stats -->
      <div id="stats-section" class="mt-10 grid grid-cols-1 sm:grid-cols-3 gap-6 text-center">
        <div class="p-4 bg-gray-50 rounded-2xl shadow-sm">
          <p class="stat-number text-2xl font-bold text-primary" data-target="50">0</p>
          <p class="text-sm font-medium text-gray-600">Active Students</p>
        </div>
        <div class="p-4 bg-gray-50 rounded-2xl shadow-sm">
          <p class="stat-number text-2xl font-bold text-primary" data-target="15">0</p>
          <p class="text-sm font-medium text-gray-600">Years Experience</p>
        </div>
        <div class="p-4 bg-gray-50 rounded-2xl shadow-sm">
          <p class="stat-number text-2xl font-bold text-primary" data-target="100">0</p>
          <p class="text-sm font-medium text-gray-600">Safety-First Training</p>
        </div>
      </div>

      <!-- CTA -->
      <div class="m-auto pt-6">
        <div class="lg:min-w-max flex items-center sm:w-max w-full pb-6 lg:pb-0 border-b border-box-bg lg:border-0 px-6 lg:px-0">
          <BtnLink
            text="Book Your Free Trial"
            href="#cta"
            className="flex justify-center w-full sm:w-max"
            variant="primary"
          />
        </div>
      </div>
    </div>

    <div class="max-w-md mx-auto midmd:max-w-none lg:mx-0 flex-1 flex w-11/12 midmd:w-5/12 lg:w-1/2 midmd:h-auto">
      <InstagramEmbed />
    </div>
  </Container>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const counters = document.querySelectorAll(".stat-number");
    let hasRun = false;

    const animateCounter = (counter: HTMLElement) => {
      // const target = +counter.getAttribute("data-target");
      const target = Number(counter.getAttribute("data-target"));
      const duration = 2000; // total duration in ms
      const start = performance.now();

      const updateCount = (now: number) => {
        const elapsed = now - start;
        const progress = Math.min(elapsed / duration, 1);

        // Ease-out cubic function
        const easedProgress = 1 - Math.pow(1 - progress, 3);

        const current = Math.floor(easedProgress * target);
        counter.innerText = current + (progress === 1 ? (target === 100 ? "%" : "+") : "");

        if (progress < 1) {
          requestAnimationFrame(updateCount);
        }
      };

      requestAnimationFrame(updateCount);
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting && !hasRun) {
          counters.forEach(counter => animateCounter(counter));
          hasRun = true;
        }
      });
    }, { threshold: 0.3 });

    const statsSection = document.getElementById("stats-section");
    if (statsSection) observer.observe(statsSection);
  });
</script>